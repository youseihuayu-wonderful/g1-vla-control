# 博士研究进展报告

## 基于 LGG100 视觉—语言—动作模型的 Unitree G1 双臂自适应调速与安全控制研究

**学生姓名：**【请填写】  
**学号：**【请填写】  
**导师：**【请填写】  
**合作研究人员：** Gaoyi、Yuhao  
**所属项目/课题组：**【请填写】  
**报告日期：**【请填写】

---

> **配置更正：** Yuhao 后续确认该 checkpoint 使用 `pi05_g1_eef`、`action_horizon=32` 和 `discrete_state_input=False`。此前的 50-step/discrete-state 结果现仅保留为历史诊断，不能用于评价模型任务能力；官方核心配置的重新验证正在进行。公开仓库还确认图像应采用 `resize_with_pad(224,224)`，而不是中心裁剪。

## 摘要

本阶段研究围绕 Gaoyi 的 LGG100 视觉—语言—动作模型（Vision-Language-Action Model, VLA）展开，目标是将该模型接入 Unitree G1 双臂机器人控制系统，并研究一种面向抓取任务的自适应调速方法。该方法不改变 VLA 生成的空间运动路径，而是根据机器人与目标物体的距离、任务阶段、接触状态、跟踪误差及运动学安全条件，对动作序列进行时间重参数化：在远距离自由运动阶段适当加速，在接近、抓取和放置等精细操作阶段减速，并在观测超时、逆运动学失败、潜在碰撞或状态异常时进入保持状态。

目前已完成真实 LGG100 checkpoint 的严格恢复和 GPU 推理、模型输出语义的初步验证、Unitree G1 MuJoCo 仿真接入、自适应调速模块、安全检查链路及闭环诊断。实验结果表明，自适应调速模块能够实现预期的阶段性速度变化：近距离接近和抓取阶段约为 0.5 倍速度，远距离自由运动阶段最高约为 1.1 倍速度。当前 59 项自动化测试全部通过。

但是，LGG100 尚未在现有 MuJoCo 场景中完成稳定的抓取与堆叠任务；完整控制链路的动作提交延迟约为 168.1 ms，超过当前设置的 100 ms 安全限制。因此，本阶段结果证明了调速与安全机制的可行性，但尚不能证明端到端任务成功或真机执行安全性。目前所有神经网络动作均保持隔离，未向 Unitree G1 真机发送动作。

**关键词：** 视觉—语言—动作模型；Unitree G1；双臂机器人；自适应调速；抓取；轨迹时间参数化；MuJoCo；安全控制

---

## 1. 研究背景

视觉—语言—动作模型能够根据相机图像、机器人状态和自然语言指令直接生成机器人动作，为通用机器人操作提供了新的研究方向。然而，将 VLA 模型接入真实双臂机器人时，仍需解决动作语义、坐标系、实时性、碰撞风险和底层运动限制等问题。

本研究使用的 LGG100 模型面向彩色方块堆叠任务。模型接收高位相机、左右腕部相机、机器人双臂状态和任务语言指令，并输出双臂末端执行器与夹爪的动作序列。由于模型作者尚未公开完整的训练配置、输入输出转换和控制代码，不能仅根据输出形状直接将动作发送给机器人。因此，本研究首先对模型输出进行语义和安全验证，再研究如何在保持空间路径不变的条件下提高机器人抓取任务的整体执行效率。

---

## 2. 研究目标

本研究的总体目标是在保证安全性和任务成功率的前提下，提高 Unitree G1 双臂机器人执行抓取任务的整体速度。

具体目标包括：

1. 验证 LGG100 模型的加载、输入格式、输出结构和动作语义；
2. 建立 LGG100 与 Unitree G1 MuJoCo 仿真系统之间的动作接口；
3. 开发基于任务阶段和环境状态的 Adaptive 自适应调速模块；
4. 实现逆运动学、连续路径碰撞、跟踪误差和运动包络等安全检查；
5. 比较 Adaptive 开启和关闭时的任务成功率、安全性与完成时间；
6. 在仿真、实时性和硬件安全条件全部满足后，再开展真机 Shadow 和低速测试。

本研究的核心思想为：

> LGG100 决定机器人手臂的运动方向和空间路径，Adaptive 模块决定该路径的执行速度。

---

## 3. 技术方法

### 3.1 LGG100 模型输入与输出

作者确认的 canonical 配置每次输出一个形状为 `[32,16]` 的动作序列。其中每个动作点包含：

- 左手末端位置与姿态：7 维；
- 右手末端位置与姿态：7 维；
- 左右夹爪命令：2 维。

正式 deployment cadence 已确认为 15 Hz，因此相邻动作点的标称时间间隔为：

\[
\Delta t = \frac{1}{15} \approx 0.067\;\text{s}
\]

早期 30 Hz 图表仅保留为历史分析，不能作为 formal Adaptive-OFF/ON timing evidence。

当前公开数据实验支持以下动作解释：

- 动作为绝对末端位姿，而非增量位姿；
- 四元数顺序为 `xyzw`；
- 动作排列为左手、右手和两个夹爪。

该解释具有较强实验支持，但仍需 LGG100 官方配置和推理代码进行最终确认。

### 3.2 自适应调速方法

Adaptive 模块只修改动作时间戳，不修改 LGG100 输出的位置和姿态。设速度比例为 \(s\)，新的动作时间间隔计算为：

\[
\Delta t_{new} = \frac{\Delta t_{original}}{s}
\]

当 \(s=0.5\) 时：

\[
\Delta t_{new} = \frac{0.033}{0.5} \approx 0.067\;\text{s}
\]

因此，同一段空间路径需要约两倍时间完成，相当于以约 0.5 倍速度执行。这里的 0.5 是无单位的时间缩放比例，不是 0.5 m/s。

当前阶段采用的主要调速规则如下：

| 任务状态 | 调速策略 |
|---|---|
| 远距离自由空间，且所有安全条件满足 | 允许适当加速 |
| 接近目标物体 | 保守减速 |
| 抓取或放置 | 约 0.5 倍速度 |
| 接触、低间隙或跟踪误差增大 | 禁止加速并减速 |
| 超时、IK 失败、碰撞或状态异常 | 立即 hold |

目前使用的 0.5 是仿真阶段的保守工程参数，并非 LGG100 学习得到的参数，也不是 Unitree 官方硬件限制。后续将通过不同速度比例的系统实验确定更合适的数值。

### 3.3 安全控制流程

当前系统采用 fail-closed 设计，主要流程为：

```text
三相机图像与机器人状态
            ↓
        LGG100 推理
            ↓
动作形状、有限值与四元数检查
            ↓
任务阶段、距离、接触和跟踪误差判断
            ↓
      Adaptive 时间重参数化
            ↓
逆运动学与连续路径碰撞检查
            ↓
末端和 14 个手臂关节的运动限制
            ↓
执行短动作前缀并持续监控
            ↓
      任一异常立即 hold
```

安全检查包括：

- 动作形状及 NaN/Inf 检查；
- 四元数和夹爪范围检查；
- IK 可达性检查；
- 连续运动路径碰撞检查；
- 夹爪与方块的最小间隙检查；
- 接触状态检查；
- 末端速度、加速度及 jerk 限制；
- 14 个手臂关节的速度、加速度及 jerk 限制；
- 观测过期和网络超时检查；
- 机器人跟踪误差和稳定性检查。

---

## 4. 本阶段已完成工作

### 4.1 真实模型恢复与推理

已完成真实 LGG100 checkpoint 的下载、版本固定和严格参数树恢复。Yuhao 后续确认核心配置为 `pi05_g1_eef`、`action_horizon=32` 和 `discrete_state_input=False`。按该配置与公开代码中的 `resize_with_pad(224,224)` 重新运行五个公开 episode、共 50 个 observation 后，50/50 输出均为有限 `[32,16]`；稳态调用延迟 P50/P95 分别约为 80.52/81.38 ms，冷启动首调用约为 14.6 s，后者包含 JAX 编译，不能计入稳态控制。

35/50 原始输出直接满足项目当前 `1e-3` quaternion norm tolerance，50/50 满足固定的有界后处理条件。公开 `pi05_g1_eef` policy 明确要求消费者在送入 IK 前归一化非零四元数。本项目仍分别保留原始输出与后处理 hash，并继续对非有限值、近零四元数和异常幅值 fail-closed；该后处理本身不解锁真机执行。

### 4.2 动作语义验证

已使用公开数据集比较绝对/增量位置、`xyzw/wxyz` 四元数顺序和左右手排列等候选解释。正确配置下 `absolute_xyzw_left-right` 在 50/50 样本中胜出，相对第二名 margin 为 97.42%；EEF position RMSE 为 3.20 mm，姿态 geodesic mean 为 0.765°，夹爪 RMSE 为 0.0754 rad。模型离线综合 score 为 0.1740，优于 hold baseline 的 0.9621，因此预注册的单次采样离线质量标准首次通过。

需要强调的是，该结果只证明公开 episode 上的离线动作一致性，不等同于稳定抓取、闭环堆叠或真机安全。旧 horizon-50 MuJoCo artifacts 已失效，必须重新建立 Adaptive OFF 基线。

### 4.3 G1 MuJoCo 接入

已完成 LGG100 动作到 Unitree G1 双臂末端执行器和 Dex1 夹爪的仿真接口，实现了模型观测、逆运动学、关节控制和 MuJoCo 动力学之间的连接。

同时修复了模型观测初态与仿真执行初态不一致的问题。当前观测、预检查和动力学执行均绑定到同一个精确初始状态和场景配置。

### 4.4 Adaptive 模块与安全链路

已完成自适应速度决策、时间重参数化、连续路径安全检查、末端运动过滤、14 关节运动过滤和异常 hold 机制。

旧速度模块曾存在实际速度、加速度和 jerk 超限的问题，因此本研究采用了重新设计的 fail-closed 实现，而不是直接复用旧模块。

### 4.5 自动化测试

当前完整自动化测试结果为：

```text
59 / 59 tests passed
```

测试覆盖动作检查、任务阶段判断、自适应调速、IK、碰撞检测、末端与关节运动限制、超时处理、接触中止和闭环执行等功能。

---

## 5. 初步实验结果

### 5.1 阶段性调速结果

在模型观测和执行初态严格绑定后，获得以下结果：

| 场景 | 最小间隙 | 任务阶段 | 速度比例范围 | 结果 |
|---|---:|---|---:|---|
| Near | 约 49.9 mm | 接近/抓取 | 0.5 | 精细阶段减速 |
| Mixed | 约 121.6 mm | 自由运动→抓取 | 0.425–1.178 | 先加速后减速 |
| Far | 约 199.7 mm | 自由运动 | 0.685–1.096 | 远距离允许加速 |

远距离场景中的动作段完成时间相对于标称时间缩短约 4.53%。该结果证明了调速策略能够根据任务阶段和空间距离改变执行速度。

### 5.2 多周期闭环诊断

在明确标记为 paused、step-synchronous 的 MuJoCo 诊断中：

- Adaptive OFF 完成 30 个重新规划周期；
- Adaptive ON 完成 30 个重新规划周期；
- 两种模式均未发生非法碰撞或控制失效；
- 两种模式均未完成完整抓取和方块堆叠；
- 方块基本保持静止。

由于两次实验中的模型输出具有随机性，因此上述结果不是严格的配对 A/B 实验，不能用于判断 Adaptive 对任务成功率的影响。

### 5.3 实时性结果

当前实时控制链路的测量结果为：

| 环节 | 典型耗时 |
|---|---:|
| LGG100 推理 | 约 83.4 ms |
| 图像处理与推理累计 | 约 93.3 ms |
| 提交前连续路径安全检查 | 约 44.5 ms |
| 动作提交时 observation age | 约 168.1 ms |
| 当前安全上限 | 100 ms |

由于动作提交时的观测年龄超过安全上限，实时系统正确执行了 0-step hold。该结果证明 watchdog 按预期工作，同时也说明当前系统尚不满足实时执行要求。

---

## 6. 当前研究状态

| 研究内容 | 当前状态 |
|---|---|
| 真实 LGG100 模型加载与推理 | 已完成 |
| 模型输出语义分析 | 基本确认，等待官方资料最终验证 |
| G1 MuJoCo 接入 | 已完成 |
| Adaptive 调速模块 | 已完成 |
| 远处加速与近处减速 | 已在隔离仿真中验证 |
| IK、碰撞和运动限制 | 已完成 MuJoCo 验证 |
| 异常自动 hold | 已验证 |
| 多周期安全运行 | 已完成初步诊断 |
| 完整抓取与堆叠 | 尚未成功 |
| 严格 Adaptive OFF/ON A/B | 尚未完成 |
| 100 ms 实时要求 | 尚未达到 |
| G1 真机执行 | 未解锁 |

当前系统状态保持为：

```text
physical_scene_calibration_verified = false
policy_task_quality_passed          = false
g1_contract_verified                = false
g1_sim_eligible                     = false
g1_execution_enabled                = false
```

---

## 7. 当前限制与待解决问题

目前尚不能判断 LGG100 模型本身无效，原因包括：

1. 作者尚未公开完整的模型配置、输入输出转换和推理后处理代码；
2. 当前相机、光照、方块位置和 MuJoCo 场景尚未与训练环境完成正式标定；
3. 原始模型四元数输出尚未通过严格执行合同；
4. 基础 LGG100 策略尚未在当前场景中完成抓取和堆叠；
5. 实时动作提交延迟超过当前安全上限；
6. 当前 0.5 调速参数尚未经过任务成功率实验优化；
7. 真机官方运动限制、watchdog、急停和硬件在环验证尚未完成。

因此，现阶段结果应解释为调速和安全机制的初步验证，而非端到端任务成功或真机执行证明。

---

## 8. 下一阶段研究计划

下一阶段将按照以下顺序推进：

1. 获取并确认 LGG100 官方 OpenPI 版本、模型配置、输入输出定义和推理流程；
2. 完成三相机内外参、手眼关系、机器人末端坐标和 MuJoCo 场景标定；
3. 在关闭 Adaptive 的条件下，先验证原始 LGG100 的基础抓取能力；
4. 优化图像处理、模型推理、IK 和连续路径碰撞检查的计算延迟；
5. 建立固定初态、固定场景和相同动作条件下的 Adaptive OFF/ON 配对实验；
6. 比较 0.4、0.5、0.6 和 0.7 等精细阶段速度比例；
7. 评估任务成功率、总完成时间、碰撞率、跟踪误差和运动平滑性；
8. 扩展至 30/100 次随机化 MuJoCo 实验和故障注入测试；
9. 完成真机只读 Shadow、watchdog、急停和硬件在环验证；
10. 只有所有安全门均通过后，才开展 G1 真机低速运动测试。

---

## 9. 技术协作与沟通

在本阶段研究过程中，我每天与 Yuhao 保持技术沟通，主要内容包括：

- LGG100 的模型结构、输入输出和推理方式；
- OpenPI 代码和 checkpoint 恢复；
- Unitree G1 双臂动作坐标系；
- MuJoCo 相机、机器人和方块场景配置；
- Adaptive 调速逻辑和参数设计；
- IK、碰撞检测、跟踪误差和运动安全；
- 实验结果分析、问题定位和下一步技术安排。

这些持续的技术讨论帮助本研究及时识别模型接口、初始状态一致性、动态接触和实时延迟等问题，并据此调整实验设计和开发优先级。

---

## 10. 阶段性结论

本阶段已经完成真实 LGG100 模型的运行、动作语义分析、Unitree G1 MuJoCo 接入，以及 Adaptive 自适应调速和 fail-closed 安全控制链路。

实验结果证明，Adaptive 模块能够在远距离自由空间中适当提高速度，在接近、抓取和放置阶段降低速度，并在异常情况下进入 hold。当前安全与控制相关的 59 项自动化测试全部通过。

但是，LGG100 尚未在当前仿真环境中完成稳定抓取和堆叠，实时动作提交延迟也超过了当前安全上限。因此，尚不能声称 Adaptive 已提高完整任务效率，也不能进入 G1 真机运动测试。

下一阶段将重点解决官方模型接口确认、视觉与物理场景标定、基础抓取成功、实时延迟优化和严格配对实验问题，并继续坚持先仿真、后 Shadow/HIL、最后真机的安全验证顺序。

---

## 附录：面向导师汇报的简短总结

我目前正在基于 Gaoyi 的 LGG100 模型研究 Unitree G1 双臂抓取任务的自适应调速。LGG100 负责生成手臂路径，我开发的 Adaptive 模块根据距离、任务阶段和安全状态调整路径的执行时间。当前仿真已经实现远距离约 1.1 倍加速和接近、抓取阶段约 0.5 倍减速，并加入了 IK、连续路径碰撞、速度、加速度、jerk 和超时检查。59 项自动化测试全部通过。但是，LGG100 还没有在当前场景中完成稳定抓取，实时控制延迟约为 168.1 ms，也超过了 100 ms 安全限制。下一步将先确认官方模型接口并完成场景标定，让 Adaptive OFF 的基础策略成功抓取，再进行严格的 Adaptive OFF/ON 对比实验。在整个研究过程中，我每天都与 Yuhao 进行技术沟通，共同分析实验问题和确定后续方案。
