# LGG100 模型接入与机器人手臂调速工作报告

## 1. 工作目标

我目前正在基于 Gaoyi 的 LGG100 VLA 模型，研究如何让 Unitree G1 机器人的双臂更快、更安全地完成抓取任务。

我的主要思路是：

- LGG100 负责决定机器人手臂往哪里运动；
- Adaptive 调速模块负责决定手臂运动得多快；
- 手臂距离物体较远时适当加速；
- 手臂接近物体和执行抓取时减速；
- 发生超时、碰撞或动作异常时立即停止。

最终目标是在不降低抓取成功率和安全性的情况下，缩短整个抓取任务的完成时间。

## 2. 已经完成的工作

### 2.1 运行真实 LGG100 模型

我已经成功下载和加载真实的 LGG100 checkpoint，并在 GPU 服务器上完成了模型推理。

LGG100 每次输出 50 个动作点，每个动作点包含 16 个数，主要表示：

- 左手末端的位置和姿态；
- 右手末端的位置和姿态；
- 左右两个夹爪的命令。

模型可以正常输出有限数值，推理时间约为 83 毫秒。

### 2.2 分析 LGG100 的动作含义

我使用公开数据集比较了多种动作定义，包括：

- 绝对位置或相对位置；
- `xyzw` 或 `wxyz` 四元数顺序；
- 左右手的排列顺序。

当前实验结果支持：LGG100 输出的是左右手绝对末端位姿，四元数顺序为 `xyzw`。

但是，作者还没有公开完整的输入输出转换代码，因此这部分仍需要使用官方资料进行最终确认。

### 2.3 开发 Adaptive 自适应调速模块

我已经完成了 Adaptive 调速模块。

这个模块不会改变 LGG100 生成的手臂路径，只改变动作点之间的时间间隔。

例如，LGG100 原始动作频率约为 30 Hz，相邻动作点间隔约为 0.033 秒。当速度比例设置为 0.5 时，新的时间间隔约为 0.067 秒，因此同一段路径以约一半速度执行。

当前调速规则为：

- 远距离自由空间：满足安全条件时允许适当加速；
- 接近物体：自动减速；
- 抓取和放置：使用约 0.5 倍速度；
- 超时、距离过近、IK 失败、碰撞或跟踪误差过大：立即 hold。

目前的 0.5 是仿真阶段使用的保守工程参数，不是模型学习得到的，也不是 Unitree 官方规定的数值。后续还需要通过 0.4、0.5、0.6、0.7 等不同参数的对比实验选择更合适的速度。

### 2.4 建立安全检查流程

我已经加入以下安全功能：

- 模型输出形状和有限值检查；
- 四元数检查；
- IK 可达性检查；
- 连续运动路径碰撞检查；
- 夹爪与方块的距离检查；
- 接触检测；
- 末端速度、加速度和 jerk 限制；
- 14 个手臂关节的速度、加速度和 jerk 限制；
- observation 超时检查；
- 异常情况下自动 hold。

系统采用 fail-closed 设计。任何必要信息缺失或安全检查失败时，系统都不会继续执行动作。

### 2.5 完成 MuJoCo 调速测试

我在 MuJoCo 中测试了不同距离下的调速效果。

| 场景 | 机器人与方块的最小距离 | 调速结果 |
|---|---:|---:|
| 接近阶段 | 约 49.9 mm | 约 0.5 倍速度 |
| 中间阶段 | 约 121.6 mm | 约 0.425–1.178 倍速度 |
| 远距离自由空间 | 约 199.7 mm | 约 0.685–1.096 倍速度 |

远距离场景中的整段动作时间比原始时间缩短约 4.53%。

这个结果说明调速模块已经能够实现：远处加速、接近和抓取时减速、异常时停止。

### 2.6 完成多轮闭环仿真

我分别测试了：

- Adaptive OFF；
- Adaptive ON。

两种模式都能够在暂停式、step-synchronous 的 MuJoCo 诊断中安全运行 30 个重新规划周期，没有出现非法碰撞或控制失效。

但是，两种模式都还没有完成完整的方块抓取和堆叠，方块基本保持静止。

因此，目前只能说明调速模块和安全模块按照预期工作，还不能证明 Adaptive 已经缩短了完整抓取任务的时间。

### 2.7 完成实时性检查

当前实时控制流程的测量结果为：

- LGG100 推理时间：约 83.4 ms；
- 图像处理加推理：约 93.3 ms；
- 短路径安全检查：约 44.5 ms；
- 动作提交时的 observation age：约 168.1 ms；
- 当前安全限制：100 ms。

由于 168.1 ms 超过了 100 ms，系统正确地拒绝执行动作，并保持机器人当前位置。

### 2.8 完成自动化测试

当前完整自动化测试结果为：

```text
59 / 59 tests passed
```

测试覆盖了动作检查、调速逻辑、IK、碰撞、速度限制、jerk 限制、超时停止和闭环执行器等功能。

### 2.9 保持真机隔离

目前没有向 Unitree G1 真机发送任何 LGG100 动作。

所有神经网络动作仍然处于隔离状态。只有在仿真任务成功、实时性、标定和硬件安全全部通过之后，才会考虑真机运动测试。

## 3. 当前已经达到的程度

| 项目 | 当前状态 |
|---|---|
| 真实 LGG100 模型加载与推理 | 已完成 |
| LGG100 动作语义分析 | 基本确认，等待官方资料最终验证 |
| Adaptive 调速模块 | 已完成 |
| 远处加速、接近和抓取减速 | 已在 MuJoCo 中验证 |
| IK、碰撞和运动限制 | 已完成 MuJoCo 验证 |
| 异常时自动 hold | 已完成验证 |
| 多轮 MuJoCo 安全运行 | 已完成 |
| 完整抓取和堆叠任务 | 尚未成功 |
| 100 ms 实时控制要求 | 尚未达到 |
| G1 真机运动 | 尚未解锁 |

## 4. 当前主要问题

目前不能直接判断 LGG100 模型本身不行，因为还存在以下问题：

1. LGG100 作者没有公开完整的模型配置和输入输出转换代码；
2. 当前相机、光照、方块位置和仿真场景还没有与训练环境完全对齐；
3. 模型原始四元数输出还没有通过严格执行标准；
4. LGG100 在当前 MuJoCo 环境中还没有完成抓取和堆叠；
5. 当前完整控制延迟超过 100 ms 安全限制；
6. 还没有真机官方速度、加速度、力矩、watchdog 和 E-stop 的完整验证。

## 5. 下一步工作

接下来我会继续完成以下工作：

1. 获取或确认 LGG100 官方模型配置、输入输出定义和推理代码；
2. 完成相机、机器人末端和 MuJoCo 场景的标定与对齐；
3. 先关闭 Adaptive，让原始 LGG100 在可信场景中完成抓取和堆叠；
4. 优化模型推理、IK 和碰撞检查速度，使动作提交时间满足安全限制；
5. 使用相同场景、相同模型输出和相同初始状态，进行 Adaptive OFF/ON 配对实验；
6. 比较抓取成功率、任务总时间、碰撞率、跟踪误差和运动平滑程度；
7. 测试不同的接近和抓取速度比例，例如 0.4、0.5、0.6 和 0.7；
8. 完成故障注入、Shadow、HIL、watchdog 和 E-stop 测试；
9. 只有全部安全条件通过后，才进行 G1 真机低速测试。

## 6. 与 Yuhao 的技术沟通

在整个工作过程中，我每天都与 Yuhao 保持技术沟通。

我们主要讨论：

- LGG100 的模型结构和输入输出；
- OpenPI 的加载和推理方法；
- G1 双臂动作坐标系；
- 相机和 MuJoCo 场景配置；
- Adaptive 调速方法；
- IK、碰撞检测和机器人安全；
- 实验结果和问题分析；
- 下一步实验方案和技术优先级。

通过每天的技术沟通，我能够及时确认技术方向、分析实验问题并调整下一步工作计划。

## 7. 简单总结

我已经完成了 Gaoyi 的 LGG100 模型在 G1 MuJoCo 环境中的初步接入，并完成了 Adaptive 调速和安全检查模块。

目前系统可以让机器人在远距离安全区域适当加速，在接近和抓取阶段降低到约 0.5 倍速度，并在出现异常时停止。

目前还没有完成完整的方块抓取和堆叠，实时控制延迟也超过了当前安全限制。因此，当前工作仍处于隔离仿真验证阶段，没有向 G1 真机发送任何动作。

下一步将重点完成 LGG100 官方接口确认、相机和场景对齐、基础抓取任务成功、实时延迟优化，以及 Adaptive OFF/ON 的严格对比实验。
