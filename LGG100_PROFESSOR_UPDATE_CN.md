# LGG100 机器人手臂自适应调速项目进展

教授您好，

我目前正在基于 Gaoyi 的 LGG100 VLA 模型，研究如何提高 Unitree G1 机器人手臂执行抓取任务的整体速度。

我的主要思路不是让机器人在所有阶段都加速，而是根据任务阶段自动调整速度：机器人距离物体较远时加快手臂运动；接近物体和执行抓取时降低速度，以减少碰撞和抓取失败。最终目标是在保证安全性和抓取成功率的前提下，缩短整个抓取任务的完成时间。

## 已完成的工作

1. **按作者确认配置重新验证 LGG100**
   Yuhao 已确认该 checkpoint 使用 `pi05_g1_eef`、32-step horizon 和 continuous state。按正确配置及公开图像预处理重新运行五个公开 episode、共 50 个 observation 后，50/50 输出均为有限 `[32,16]`；绝对 EEF、`xyzw`、左手在前的解释在 50/50 样本中胜出，末端位置 RMSE 约为 3.20 mm，离线模型 score 明显优于 hold baseline。

2. **接入 G1 MuJoCo 仿真环境**  
   我已经将 LGG100 的输出接入 Unitree G1 的 MuJoCo 双臂仿真环境，用于隔离测试模型动作和机器人控制流程。

3. **开发 Adaptive 自适应调速模块**  
   LGG100 负责生成机器人手臂的运动路径，Adaptive 模块只调整路径的执行速度，不改变模型生成的位置和姿态。当前设计为：
   - 远距离自由运动时允许适当加速；
   - 接近物体、抓取和放置时减速；
   - 出现超时、碰撞、距离过近或动作异常时立即停止。

4. **建立安全检查流程**  
   我加入了动作格式检查、IK 可达性检查、连续路径碰撞检查、接触检测，以及末端和关节的速度、加速度与 jerk 限制。任何安全检查失败时，系统都会保持当前位置，不继续执行动作。

5. **完成仿真调速实验**  
   实验中，机器人在距离方块较远时最高可以达到约 1.1 倍速度；进入接近和抓取阶段时，速度降低到约 0.5 倍。0.5 是当前仿真阶段使用的保守参数，后续还需要通过不同速度比例的对比实验进行优化。

6. **完成历史闭环和实时性诊断**
   旧候选配置下，Adaptive 开启和关闭时系统都能在 MuJoCo 中安全运行多个重新规划周期，实时 watchdog 也会拒绝超过 100 ms 的过期动作。但现在已确认旧实验使用的 50-step/discrete-state/中心裁剪配置与训练设置不一致，因此这些结果只保留为安全机制诊断，不能用于评价 LGG100 任务能力。正确 32-step 配置的闭环基线需要重新运行。

7. **完成自动化测试**  
   目前安全控制和调速相关的 59 项自动化测试全部通过。

## 当前结论

目前已经证明 Adaptive 模块能够按照任务阶段正确调整速度，实现远处加速、接近和抓取时减速，并在异常情况下停止。

但是，目前还不能证明整个抓取任务已经变快。正确配置已经显著改善离线动作一致性，但尚未完成新的 MuJoCo 闭环抓取和堆叠；相机和仿真场景仍未完成物理标定，完整实时计算延迟也需重新测量。因此目前不能从离线误差直接推断任务成功或真机安全。

目前所有模型动作都只用于隔离仿真，没有向 G1 真机发送任何动作。

## 下一步计划

1. 继续获取精确 OpenPI commit、完整历史 TrainConfig 和实际 replanning 策略；
2. 对齐相机参数、机器人末端坐标和 MuJoCo 场景；
3. 使用正确 32-step 配置，先关闭 Adaptive，重新验证原始 LGG100 能否稳定完成抓取任务；
4. 优化模型推理、IK 和碰撞检查延迟；
5. 在基础抓取成功后，对相同场景和相同动作进行 Adaptive OFF/ON 配对实验；
6. 比较不同速度设置下的抓取成功率、任务时间、碰撞率和跟踪误差；
7. 完成仿真成功率和安全测试后，再考虑 G1 真机的只读 Shadow 测试和低速测试。

## 技术沟通

在项目进行过程中，我每天都与 Yuhao 保持技术沟通，主要讨论 LGG100 的输入输出、G1 动作坐标系、MuJoCo 场景、Adaptive 调速方法、安全检查、实验结果和下一步技术方案。这些沟通帮助我及时分析实验问题并调整工作计划。

简单总结：我已经完成了 LGG100 的仿真接入、自适应调速和安全控制流程。目前调速功能已经通过仿真验证，下一步重点是完成基础抓取任务、优化实时延迟，并验证 Adaptive 是否能够在不降低成功率的情况下缩短任务总时间。
